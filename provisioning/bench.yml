- hosts: localhost
  user: root
  gather_facts: yes

  roles:
    - bootstrap

    - vagrant-specific

    - kernel-tune
    - nginx
    - nginx.log-format
    - golang
    - external.nginx
    - external.shipment
    - external.payment
    # TODO
    # - benchmark-worker

  tasks:
    - name: Start default daemons
      become: yes
      become_user: root
      systemd:
        name: "{{ item }}"
        state: restarted
        daemon_reload: yes
        enabled: yes
      with_items:
        - nginx.service
        - shipment.service
        - payment.service
